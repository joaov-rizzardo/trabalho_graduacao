export type UserType = {
    userId: number,
    username: string,
    email: string,
    name: string,
    lastName: string,
    selectedAvatar: number,
    isValidatedEmail: boolean,
    createdAt: string
}